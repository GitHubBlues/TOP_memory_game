import React from "react";
import "./instructions.css";

const Instructions = () => {
    return (
        <div>
            <span className="instructions">
                You get one point for each card you click on! But be aware: You shoud click each card only once!
            </span>
        </div>
    );
};

export default Instructions;